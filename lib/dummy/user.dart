class User {
  int? id;
  String? name;
  String? email;
  double? latitude;
  double? longitude;

  User({this.id, this.name, this.email, this.latitude, this.longitude});

  // getDistanceBetweenPointsNew(latitud) {

  // }
}
